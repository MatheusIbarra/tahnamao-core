# Copie para .env e ajuste conforme o ambiente.
# Banco local (Docker): docker compose up -d

MONGODB_URI=mongodb://localhost:27017/tahnamao-core
PORT=3000
AUTH_ACCESS_TOKEN_SECRET=replace-with-a-long-random-secret
